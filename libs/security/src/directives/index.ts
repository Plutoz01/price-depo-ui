import { AuthenticatedDirective } from './authenticated.directive';
import { HasAnyRoleDirective } from './has-any-role.directive';
import { HasPermissionDirective } from './has-permission.directive';

export const directives = [ AuthenticatedDirective, HasAnyRoleDirective, HasPermissionDirective ];
